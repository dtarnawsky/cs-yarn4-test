import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.yarn4.test',
  appName: 'cs-yarn4-test',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
